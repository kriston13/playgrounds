<h2><%= @playground.name %></h2>

<div>


<p>Located in: <%= @playground.suburb %>, <%= @playground.state %>, <%= @playground.country %></p>
</div>

<div>
    <%= "Showing #{pluralize(@pg_pix.size, 'picture')}" %>
    <% @pg_pix.each do |pix| %>
        <p>
            Image ID is: <%= pix.id%><br />
            <%= image_tag pix.image_url(:thumb).to_s %><br />
            <%= button_to "Delete this picture", playground_pg_picture_path(@playground, pix), :method => "delete", :confirm => "Are you sure you want to delete this picture?" %>
        </p>
    <% end %>
    <p><%= link_to "Add a picture", new_playground_pg_picture_path(@playground)%>
    </p>
</div>    
    
<p><%= link_to "Return to all playgrounds", playgrounds_path %>
</p>